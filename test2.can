// Test file
/* Comment */
long msgId;
int x;
byte b;
const int y = 10;

void test() {
    while (x > 0) {
        write("test");
    }
    for (int i = 0; i < 10; i++) {
        putValue("signal", x);
    }
}